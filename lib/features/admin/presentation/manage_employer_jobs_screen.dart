// approve_jobs_screen.dart
import 'package:flutter/material.dart';

class ApproveJobsScreen extends StatelessWidget {
  const ApproveJobsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Approve Jobs'));
  }
}


// manage_employer_jobs_screen.dart
class ManageEmployerJobsScreen extends StatelessWidget {
  const ManageEmployerJobsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Manage Employer Jobs'));
  }
}
